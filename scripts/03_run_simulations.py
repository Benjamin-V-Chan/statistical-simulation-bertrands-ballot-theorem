# 1. Read `outputs/simulation_results.csv`
# 2. Count total sequences and number of sequences where A is always ahead.
# 3. Compute empirical probability: (count of "always ahead" cases) / (total sequences).
# 4. Store the empirical probability in `outputs/probability_comparison.csv`
